# Keep the Docker build context small and exclude build artifacts. The waserver
# container image only needs the Rust workspace crates (Cargo.toml, Cargo.lock,
# waserver/, waclient/).
target
**/target
.git
android
assets
demo
integrations
*.md
**/compose.yaml
